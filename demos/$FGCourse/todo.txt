- Implement Memory checking functions, use it in one manager's allocations
	- use for MeshManager and TextureManager at least? log the allocations into the log
- Implement serialization (ReadFrom, WriteTo file) for Mesh class
	- how? we need to serialize mesh data.
- Implement Thread and Mutex functionality and run one of your message-supported Managers in its own thread, communicating only with messages (Render-thread? audio-thread?)
- Implement Message class, including 2 sub-classes, and use them in at least 1 communication pattern between Managers in your project
- Write a small Game Engine Architecture document (2 pages minimum)
Bonus:
- .obj-file loader implemented, including Vertices and Faces
- add and delete entities in the editor

